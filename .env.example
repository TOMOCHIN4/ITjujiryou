# IT十字陵 環境変数サンプル
#
# 認証は Claude Max プランの OAuth ログインを利用します。
# 事前に `claude login` で Anthropic アカウント (Max プラン) にログインしてください。
# 本システムは ANTHROPIC_API_KEY を使用しません（.env に書く必要はありません）。

# ITJUJIRYOU_DB_PATH=data/office.db
# ITJUJIRYOU_LOG_PATH=data/logs/timeline.log
